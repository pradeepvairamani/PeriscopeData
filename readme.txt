→ python --version
Python 3.5.1 :: Anaconda 2.4.1 (x86_64)

To run the code:
Navigate to the location of periscope.py and run the command `python periscope.py`

To run the unittest:
Run the command `python -m unittest test_periscope.py`